# UI Test Dependencies
# Install with: pip install -r requirements-ui-test.txt

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-timeout>=2.1.0

# Headless testing support (Linux only)
pytest-xvfb>=2.0.0; sys_platform == "linux"

# Coverage reporting
coverage>=7.3.0

# Type checking (optional but recommended)
pytest-mypy-plugins>=3.0.0; python_version >= "3.8"

# Additional utilities
Pillow>=10.0.0  # For screenshot comparison (optional)
